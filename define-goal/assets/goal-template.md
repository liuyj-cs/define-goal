# <<TITLE>>

> 状态：已批准  
> 创建日期：<<DATE>>  
> 目标所有者：<<OWNER>>  
> 目标文档：`<<RELATIVE_PATH>>`  
> 交接原则：本文件是完整目标契约，可由任意获准访问本仓库的 Agent 接手，不依赖定义会话。

## 目标陈述

<<GOAL_STATEMENT>>

## 为什么要做

<<WHY>>

## 已知事实与来源

<<CURRENT_STATE>>

这里记录的是定义目标时观察到的事实，不保证执行时仍未变化；每条易漂移事实必须注明来源和观察日期。

## 已确认决策、假设与留白

### 已确认决策

<<DECISIONS>>

### 代拍的板（发出前可改判）

执行者按以下默认执行，所有者批准前可改判。

<<AGENT_DECIDED_DEFAULTS_WITH_REASON_AND_COST>>

### 假设及猜错的影响

<<ASSUMPTIONS>>

### 明确不采用或暂缓

<<REJECTED_OR_DEFERRED>>

## 相关目标

- 关联目标与先后顺序：<<RELATED_GOALS_AND_ORDER>>
- 重叠路径与归属：<<OVERLAPPING_PATHS_AND_OWNERSHIP>>
- 接缝与交接条件：<<SEAMS_AND_HANDOFF_CONDITIONS>>

## 范围与权限

### 范围内

<<IN_SCOPE>>

### 范围外

<<OUT_OF_SCOPE>>

### 允许的变更与外部动作

<<AUTHORIZED_ACTIONS>>

完成范围内工作所需的机械性小外溢（如同步 import、更新引用）允许，随手在进度中记一句即可；实质性的范围变更才停下询问所有者。

## 约束、优先级与防绕过

- 冲突时的优先级：<<PRIORITY_ORDER>>
- 必须遵守：<<HARD_CONSTRAINTS>>
- 不得用这些方式“达标”：<<ANTI_CHEAT>>
- 执行前已有、不得清理或覆盖的工作树状态：<<PREEXISTING_WORKTREE_STATE_OR_NONE>>

## 执行前复核

- [ ] 读取执行环境中当前生效的 `AGENTS.md`、`CLAUDE.md` 及仓库规则，采用适用于当前路径的最具体规则。
- [ ] 记录当前分支与工作树；不得修改、清理或提交范围外的既有变更。若既有变更与目标范围重叠且无法确认所有权，冻结重叠文件、继续其余部分并在进度中报告；仅当重叠部分就是目标主体时才停止。
- [ ] 重新运行本目标依赖的基线或事实检查，不把定义阶段的观察当作当前事实。
- [ ] 预跑完成契约中的全部命令（如 --version 或空跑）确认工具可用；缺失的常见开源工具按"代拍的板"的授权安装并记入进度，装不出来的（凭据、外部服务等）把受影响项标为未验证后继续其余部分。
- [ ] 若复核结果与本文件的关键前提冲突，记录实际证据并按“停止、升级与续跑”处理；不得静默改写目标。
- [ ] 动工前把理解的目标、执行顺序和最大风险写成不超过 10 行的开工回执；回执与本文件冲突时先重读本文件自行消解，仅确认真实矛盾时才停下询问。
<<ADDITIONAL_PREFLIGHT>>

## 工作与取证路径

本节的取证与证据要求是硬性的；步骤与顺序是建议；偏离须在进度文件记一句原因，且不得因偏离少交任何证据。

<<WORK_AND_EVIDENCE_PLAN>>

## 完成契约

- [ ] AC-01 结果标准：<<OUTCOME_CRITERION>>；证据：<<OUTCOME_EVIDENCE>>
- [ ] AC-02 约束标准：<<CONSTRAINT_CRITERION>>；证据：<<CONSTRAINT_EVIDENCE>>
<<ADDITIONAL_CRITERIA>>

只有把每条标准对应的实际命令、输出、文件或来源证据放进执行对话，才算完成；只口头声称“已完成”不算。

## 停止、升级与续跑

- 遇到以下情况停止继续扩张并如实报告：<<STOP_CONDITIONS>>
- 依赖或凭据缺失时，先完成不受影响部分、把受影响项标为未验证，然后按停止条件收尾并如实报告；这就是该情形下合法的结束方式。
- 达到以下预算即交付当前最优结果：<<BUDGET>>
- 非承重疑问记入进度文件的“待裁决”区，跳过后继续做不受影响的部分；只有命中停止条件或承重前提冲突才全局停止。
- 需要跨会话时，把已完成项、证据、失败尝试和下一步写入 `<<PROGRESS_PATH>>`；进度文件不得修改本目标契约。
- 如需改变本目标，先记录变更原因并获得目标所有者确认。

## 跨 Harness 交接

- 本文件是执行契约；定义会话的聊天记录、scratchpad、临时计划和 goal 状态都不是契约的一部分。
- 本文件状态为“已批准”时方可执行；“已完成”“已废弃”仅供只读复核，须所有者明确重新开启（状态改回“已批准”）后才能再次执行。
- 接手 Agent 必须从仓库根目录读取本文件及当前生效的仓库规则，再执行“执行前复核”。
- 文件路径、命令和证据要求均以仓库相对位置表达；若目标 Harness 无法访问仓库，按停止条件报告；缺工具或凭据按“执行前复核”的预跑规则处理，不得靠猜测填补。
- 定义 Agent 与执行 Agent 可以不同；任何 Harness 都不得因为自己没有参与定义而自行补写目标含义。

在支持 `/goal` 的 Codex 或 Claude Code 中，从目标仓库启动：

```text
/goal 读取并完成 `<<RELATIVE_PATH>>` 中已批准的目标。把该文件视为结果、范围、权限、约束和验收的完整契约；先执行“执行前复核”，再持续工作，直到“完成契约”每一项都有实际证据出现在执行对话中，或命中已记录的停止条件。
```

完成契约全部拿到证据后，执行 Agent 不得自行结项：须立即派一个未参与执行的子 agent 独立验收——亲自复跑完成契约命令、临场抽查，全部通过即翻状态并追加结项记录——并把验收报告原样转交所有者，不得改写或摘要。验收只做完成性验证，不是代码评审，不产出逐项审查文书。验收报告按结果定长短：全部通过只报一两句（独立验收通过、复跑与抽查数量、已结项），不逐项展开；仅对未通过或未验证项展开细节。子 agent 能加载 define-goal skill 时按其验收流程执行，加载不到时按本段要求执行即可。宿主无法派子 agent 时，退回人工方式：在最后一条回复中原样给出下面这行命令，供所有者复制到新会话。

```text
/define-goal 验收 <<RELATIVE_PATH>>
```

（Codex 中用 `$define-goal 验收 <<RELATIVE_PATH>>`。）
