# 完整新模板示例

> 状态：已批准
> 目标类型：执行型
> 创建日期：2099-01-01
> 目标所有者：Fixture Owner
> 目标文档：`docs/goals/valid-full-template.md`
> 交接原则：本文件是完整目标契约，可由任意获准访问本仓库的 Agent 接手，不依赖定义会话。

## 目标陈述

交付一个可验证的本地示例。

## 为什么要做

证明新增模板字段可以被完整填写。

## 已知事实与来源

- 2099-01-01 由本地 fixture 观察：示例不访问外部系统。

## 已确认决策、假设与留白

### 已确认决策

- 仅创建本地文本产物。

### 代拍的板（发出前可改判）

执行者按以下默认执行，所有者批准前可改判。

- 默认使用 UTF-8；理由：仓库文本统一；猜错代价：重新编码文件。

### 假设及猜错的影响

- 假设 Python 可用；猜错则停止并报告。

### 明确不采用或暂缓

- 不访问网络。

## 相关目标

- 关联目标与先后顺序：无前置目标，本目标独立开始。
- 重叠路径与归属：示例文件仅归本目标修改。
- 接缝与交接条件：验证通过后交给所有者确认。

## 范围与权限

### 范围内

- 本地示例文件。

### 范围外

- 外部系统。

### 允许的变更与外部动作

- 允许本地只读验证。

## 约束、优先级与防绕过

- 冲突时的优先级：正确性优先。
- 必须遵守：不访问网络。
- 不得用这些方式“达标”：不得跳过验证。
- 执行前已有、不得清理或覆盖的工作树状态：无。

## 执行前复核

- [ ] 读取仓库规则。
- [ ] 记录工作树。
- [ ] 重跑基线。
- [ ] 前提冲突时停止。
- [ ] 动工前用不超过 10 行写明理解的目标、执行顺序和最大风险；若回执与本文件冲突，先停再动工。

## 工作与取证路径

本节的取证与证据要求是硬性的；步骤与顺序是建议；偏离须在进度文件记一句原因，且不得因偏离少交任何证据。

1. 生成示例并运行验证器。

## 完成契约

- [ ] 结果标准：示例验证通过；证据：验证器退出 0。
- [ ] 约束标准：未访问外部系统；证据：执行记录。

只有把每条标准对应的实际命令、输出、文件或来源证据放进执行对话，才算完成；只口头声称“已完成”不算。

## 停止、升级与续跑

- 遇到以下情况停止继续扩张并如实报告：Python 不可用。
- 达到以下预算即交付当前最优结果：一次有界实现。
- 非承重疑问记入进度文件的“待裁决”区，跳过后继续不受影响的部分；只有命中停止条件或承重前提冲突才全局停止。
- 需要跨会话时，把证据写入 `docs/goals/valid-full-template.progress.md`；进度文件不得修改本目标契约。
- 如需改变本目标，先获得所有者确认。

## 跨 Harness 交接

- 本文件是执行契约；定义会话上下文不是契约的一部分。
- 接手 Agent 重新读取规则并执行复核。
- Harness 无法访问工具时按停止条件报告。

```text
/goal 读取并完成 docs/goals/valid-full-template.md 中已批准的目标。
```
